<odoo>
    <data>
        <record id="view_move_form" model="ir.ui.view">
            <field name="name">view.move.form</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <!-- add tax_invoice_id after payment_term_id and hide Create and Edit -->
                <xpath expr="//div[@name='journal_div']" position="after">
                    <field name="tax_invoice_id" options="{'no_create': True}"
                           domain="[('is_used', '=', False)]"
                           attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund', 'out_receipt'))]}"/>
                </xpath>

                <!-- add tax_invoice_no in Vendor Bills Form -->
                <xpath expr="//div[@name='journal_div']" position="after">
                    <field name="tax_invoice_no"
                           attrs="{'invisible': [('move_type', 'not in', ('in_invoice', 'in_refund', 'in_receipt'))]}"/>
                </xpath>

                <xpath expr="//field[@name='ref']" position="after">
                    <field name="ar_receipt_type"
                           attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund', 'out_receipt'))]}"/>
                </xpath>

                <!-- add new page inside notebook -->
                <notebook position="inside">
                    <page string="E-Faktur">
                        <group>
                            <group>
                                <field name="masa_pajak"/>
                                <field name="tahun_pajak"/>
                            </group>
                            <group>
                                <field name="is_efaktur_exported"/>
                                <field name="date_efaktur_exported"/>
                            </group>
                        </group>
                    </page>
                </notebook>
            </field>
        </record>

        <record id="view_out_invoice_tree" model="ir.ui.view">
            <field name="name">view.out.invoice.tree</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_out_invoice_tree"/>
            <field name="arch" type="xml">
                <!--  add field on tree list view customer invoice-->
                <xpath expr="//field[@name='invoice_date_due']" position="after">
                    <field name="tax_invoice_id" optional="show"
                           attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund', 'out_receipt'))]}"/>
                </xpath>
            </field>
        </record>

        <record id="view_in_invoice_tree" model="ir.ui.view">
            <field name="name">view.in.invoice.tree</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_in_invoice_tree"/>
            <field name="arch" type="xml">
                <!--  add field on tree list view supplier invoice-->
                <xpath expr="//field[@name='invoice_date_due']" position="after">
                    <field name="tax_invoice_no" optional="show"
                           attrs="{'invisible': [('move_type', 'not in', ('in_invoice', 'in_refund', 'in_receipt'))]}"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
